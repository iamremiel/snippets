print_r(array_filter($linksArray));

or you can use

 foreach ($combined_arrays as $key => $value) {
                if (is_null($value) || $value) {
                   unset($arrays[$key]);
                }
